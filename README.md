To get started, copy `docker-compose.yml.example` to `docker-compose.yml`\
Then replace the filler token space with your Discord bot token

To run the project, simply execute\
`./start.sh`
From the project root